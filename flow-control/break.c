#include<stdio.h>
int main(){

    int a,b,i;
    // printf("\nEnter a number to check positive number");
    // scanf("%d",&a);
    printf("\nhi");
    // for loop with break statment
    for(i=-1;i<=0;i++){
        if(i<0){
            printf("\ninside if statement");
            break;
        }
        printf("\ninside for loop");
    }
    return 0;
}