#include<stdio.h>
#include<stdlib.h>
struct emp{
    int id;
};

int main(){
    int *p;

    struct emp *e;
    e =(struct emp *)malloc(sizeof(struct emp));
    
    p=(int *)malloc(sizeof(int));


return 0;
}